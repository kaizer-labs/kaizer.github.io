---
interface Item {
  title: string;
  body: string;
}

interface Props {
  items: Item[];
}

const { items } = Astro.props;
---

<div class="values-grid">
  {items.map((item) => (
    <article class="value-card">
      <h3>{item.title}</h3>
      <p>{item.body}</p>
    </article>
  ))}
</div>
